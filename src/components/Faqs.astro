---
const faqs = [
  {
    pregunta: "¿Necesito ser arquitecto o diseñador para hacer esta formación?",
    respuesta: "No. No se requieren estudios previos en diseño o arquitectura. La formación está pensada para cualquier persona que pueda interpretar un plano básico. ¡Si te interesa armonizar espacios, podés hacerla!"
  },
  {
    pregunta: "¿Y si no tengo conocimientos sobre Vastu Shastra?",
    respuesta: "No te preocupes. Comenzamos desde cero. Vas a aprender desde un nivel introductorio hasta un nivel avanzado, con el objetivo de que termines capacitado como Consultor en Vastu Shastra."
  },
  {
    pregunta: "¿Qué pasa si no tengo tiempo?",
    respuesta: "La formación está diseñada para que la hagas a tu ritmo. Todas las clases están grabadas, y podés verlas cuando quieras. Además, tenés material gráfico de apoyo y sesiones en vivo permanentes para resolver dudas."
  },
  {
    pregunta: "¿Y si no tengo el dinero para pagarla de una?",
    respuesta: "¡Hay facilidades de pago! Podés abonar la formación en 3 pagos sin interés, para que acceder sea mucho más fácil."
  }
];

---

<section id="faqs" class="relative py-[60px] lg:py-[80px] px-4">
    <h2 class="text-[32px] text-white md:text-[42px] font-semibold mb-4 text-center">Preguntas Frecuentes</h2>
    <div class="max-w-[800px] mx-auto" id="faq-container">
      {
        faqs.map((item, index) => (
          <div
            class="faq-item w-full cursor-pointer p-[20px] border-b border-b-white/50 last:border-b-0"
            data-index={index}
          >
            <h3 class="faq-question font-semibold text-[18px] md:text-[20px] text-white flex justify-between items-center">
              {item.pregunta}
              <svg class="faq-icon min-w-[20px] size-[20px] transition-all duration-300" fill="#FFF" viewBox="0 0 448 512"><path d="M256 80c0-17.7-14.3-32-32-32s-32 14.3-32 32V224H48c-17.7 0-32 14.3-32 32s14.3 32 32 32H192V432c0 17.7 14.3 32 32 32s32-14.3 32-32V288H400c17.7 0 32-14.3 32-32s-14.3-32-32-32H256V80z"/></svg>
            </h3>
            <div class="faq-answer normal-case overflow-hidden duration-500 transition-all max-h-0">
              <p class="text-white/80 text-[18px] pt-[10px]">{item.respuesta}</p>
            </div>
          </div>
        ))
      }
    </div>
</section>

<script is:inline>
  const faqItems = document.querySelectorAll(".faq-item");

  faqItems.forEach(item => {
    const question = item.querySelector(".faq-question");
    const answer = item.querySelector(".faq-answer");
    const icon = item.querySelector(".faq-icon");

    question.addEventListener("click", () => {
      const isOpen = answer.classList.contains("max-h-[2000px]");

      document.querySelectorAll(".faq-answer").forEach(ans => ans.classList.remove("max-h-[2000px]"));
      document.querySelectorAll(".faq-icon").forEach(i => i.classList.remove("rotate-45"));

      if (!isOpen) {
        answer.classList.add("max-h-[2000px]");
        icon.classList.add("rotate-45");
      }
    });
  });
</script>
