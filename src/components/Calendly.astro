<section class="py-8 px-4 bg-[#87806B]" id="calendly">
	<div class="max-w-[600px] mx-auto">
		<div class="flex mx-auto justify-center mb-4 gap-2">
			<img src="/images/reviews-profiles.png" alt="Reviews Vastu Shastra">
			<div class="flex items-center gap-2">
				<p>5 de 5</p>
				<img class="h-[10px]" src="/images/estrellas.webp" alt="Estrellas">
			</div>
		</div>
		<h2
			class="text-[20px] text-center md:text-[32px] leading-[120%] md:leading-[110%] font-bold text-white mb-4"
		>
			Agendá tu llamada, vamos a resolver tus dudas y consultas, y ver
			juntos si esta Formación es para vos.
		</h2>
		<div class="bg-white text-[#3a3628] rounded-lg px-4 py-3 text-center shadow-md border-2 border-[#D4B26A]">
			<p class="text-[16px] md:text-[18px] font-semibold leading-tight">
				🎁 <span class="text-[#B8860B] font-bold">20% OFF</span> — Solo para quienes agenden su llamada hoy.
			</p>
		</div>
		<iframe
			id="calendly-frame"
			class="w-full mt-8 min-h-[1300px]"
			title="Calendly - Consulta Online Formación Vastu Shastra"
			loading="lazy"
			allowtransparency="true"></iframe>
		<p class="text-center text-white mt-4 px-2">En caso de no encontrar horarios disponibles escribime por WhatsApp al <a class="text-blue-700 underline" href="wa.me/+5491121937854">+54 9 11 2544-0011</a></p>
	</div>
	<img
		class="h-[60px] mt-8 md:h-[60px] mx-auto mb-8"
		src="/images/vastu-shastra.webp"
		alt="Ivan Vastu Shastra"
	/>

	<p class="text-white/80 text-center mt-8 text-[14px] md:text-[16px]">
		© Formación Vastu Shastra 2025. Todos los derechos reservados.
	</p>
	<p class="text-white/60 text-[12px] text-center">
		Este sitio no forma parte de Facebook™ o Meta Platforms, Inc. La
		información presentada tiene fines educativos y no garantiza resultados
		específicos.
		<!-- Al registrarte, aceptás nuestros
		[Términos y Condiciones] y [Política de Privacidad]. -->
	</p>
</section>

<script is:inline>
	const calendalyBase = "https://calendly.com/dacharryi/consulta-online-formacion-vastu-shastra"

	// Se ejecuta únicamente en el navegador
	const name = localStorage.getItem("lead_name") || "";
	const email = localStorage.getItem("lead_email") || "";

	const params = new URLSearchParams({
		hide_gdpr_banner: "1",
		embed_domain: "formacionvastushastra.com",
		embed_type: "Inline",
		name,
		email,
	});

	document.getElementById("calendly-frame").src =
		calendalyBase + "?" + params.toString();

	// --------------
	
	
	function getCookie(name) {
		const m = document.cookie.match(
			new RegExp("(?:^|; )" + encodeURIComponent(name) + "=([^;]*)"),
		);
		return m ? decodeURIComponent(m[1]) : "";
	}

	// Fallback: si no hay cookie, intentamos inferir desde localStorage con la misma lógica del modal
	function isQualifiedFallbackFromStorage() {
		try {
			const answers = JSON.parse(
				localStorage.getItem("qf_answers") || "{}",
			);
			const ocup = String(answers.ocupacion || "");
			const comp = String(answers.compromiso || "");
			const isTarget = ocup === "arquitecta" || ocup === "disenadora" || ocup === "diseñadora";
			const hasCommitment = comp !== "explorando";
			const pres = String(answers.presupuesto || "");
			const canPay = pres !== "no-ahora";
			return isTarget && canPay;
		} catch {
			return false;
		}
	}

	function isQualified() {
		const statusCookie =
			getCookie("qf_status") || localStorage.getItem("qf_status") || "";
		if (statusCookie) return statusCookie === "qualified";
		return isQualifiedFallbackFromStorage();
	}

	// Crea un ID legible si Calendly no trae UUID (máx 128 chars)
	function fallbackEventId() {
		return (
			"cal_" +
			Date.now().toString(36) +
			"_" +
			Math.random().toString(36).slice(2, 10)
		);
	}

	// Sanitiza a caracteres válidos para Meta (recomendado)
	function sanitizeId(id) {
		return String(id)
			.replace(/[^a-zA-Z0-9_\-:.]/g, "")
			.slice(0, 128);
	}

	async function sendMetaSchedule(payload) {
		try {
			await fetch("/api/meta-capi", {
				method: "POST",
				headers: { "Content-Type": "application/json" },
				body: JSON.stringify(payload),
				keepalive: true,
			});
		} catch (e) {
			console.error("Error enviando a Meta CAPI:", e);
		}
	}

	console.log("Califica?", isQualified());

	const isQ = isQualified();
	const eventId = fallbackEventId();

	// Datos complementarios
	const phone = localStorage.getItem("lead_phone") || "";
	const fbp = getCookie("_fbp") || localStorage.getItem("fbp") || "";
	const fbc = getCookie("_fbc") || localStorage.getItem("fbc") || "";

	console.log(
		"Data para Meta: ",
		email,
		" - ",
		phone,
		" - ",
		fbp,
		" - ",
		fbc,
	);

	window.addEventListener("message", async function (e) {
		if (e.origin !== "https://calendly.com") return;
		if (!e?.data || typeof e.data !== "object") return;

		const evt = e.data.event || "";
		if (typeof evt !== "string" || !evt.startsWith("calendly.")) return;

		if (evt === "calendly.event_scheduled") {
			// Marcamos en el Sheet que si agendo
			fetch(
				"https://hook.us2.make.com/5vkgddg57kuana2jmn2evmvhch3ws8p8",
				{
					method: "POST",
					headers: { "Content-Type": "application/json" },
					body: JSON.stringify({
						email,
					}),
					keepalive: true,
				},
			).catch((err) => console.error("Tracking error:", err));

			// Marcar agendo=Si en FFA (upsert por correo)
			// keepalive: true → la request sobrevive al redirect a /thankyou
			fetch("/api/analytics/lead", {
				method: "POST",
				headers: { "Content-Type": "application/json" },
				body: JSON.stringify({
					name,
					email,
					phone,
					agendo: "Si",
				}),
				keepalive: true,
			}).catch((err) => console.error("[FFA] error:", err));

			// 👇 Enviar SOLO si está calificado
			if (isQ) {
				console.log("entra al evento");
				await sendMetaSchedule({
					eventName: "Schedule",
					eventTime: Math.floor(Date.now() / 1000),
					eventSourceUrl: window.location.href,
					actionSource: "website",
					eventId,
					email,
					phone,
					fbp,
					fbc,
				});
			}

			// Redirigir siempre al thankyou (califique o no)
			setTimeout(() => {
				const ty_url = "/thankyou?event_id=" + eventId;
				window.location = ty_url;
			}, 4000);
		}
	});

	window.addEventListener("load", () => {
		fbq("track", "Lead");
	});
</script>
